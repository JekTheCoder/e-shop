import { RemoveFilterPipe } from './remove-filter.pipe';

describe('RemoveFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
